# ⚠ 这个仓库**只收代码与文档**。语料、密钥、产出、模型输出一律不进。
#   下面这些在本机都存在（开发时要用），但**永远不该被提交**。
#   宁可写宽 —— 漏进来一条语料是不可逆的（推送后能被人 clone 走）。

# ---- 凭据 ----
.env
api-keys/
*.key
*credentials*

# ---- 语料（第三方版权，绝不能公开）----
手上有的/语料/
手上有的/法语全量语料/
手上有的/俄语全量语料/
手上有的/俄语测试语料/
新的/
corpus/
*.srt
*.m4a
*.wav
*.WAV
*.aac
*.mp3
*.flac
*.mp4

# ---- 产出与模型输出（都含语料原句）----
out/
cache/
logs/
bakeoff/
archive/
*.zip
*.xlsx
*.xls
*.jsonl

# ---- 内部文档 ----
飞书文档/
arena排行榜/
计划_*.md
交付说明_*.md
提示词v2_回归判据.md
models_tmp.json
*.png
*.jpg
# 会话实录、随手笔记之类。Claude Code 会自动往工作目录丢这种 .txt，
# 内容是**整段对话**（含语料原句），文件名却是 ASCII 的、看着人畜无害。
# 2026-09-20 就有一个 1.19 MB 的躺在仓库根下，差点被 add 进来。
#
# ⚠ 精确匹配它「日期-时间-开头50字」的形状，**不要写成笼统的 `*.txt`** ——
#   本仓库根下有一个正当的 requirements.txt，会被一起挡掉（实测踩过一次，
#   靠「待提交清单里核对文件数」才发现）。
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
whisperx-env/
.pytest_cache/
