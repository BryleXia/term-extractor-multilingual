# 术语抽取流水线 · 运行时依赖
#
# 下列版本是**实测过的组合**：922 条断言 + 六个 e2e 脚本全绿。
# 用 >= 而不是 == ，是为了让别的环境也装得上；要完全锁死请自行 freeze。

openpyxl>=3.1.5          # 读写 .xlsx：交付术语表、QC 表
openai>=2.23.0           # 调用模型（llm.py 用 AsyncOpenAI）
httpx>=0.28.1            # e2e 脚本给 HTTP 层打桩用，全程零 API 调用
simplemma>=2.0.0         # 罗曼语（西语 / 法语）词典形校验与还原
pymorphy3>=2.0.6         # 俄语形态分析：6 个格还原到主格
pymorphy3-dicts-ru       # ↑ 的俄语词典。**是独立包**，不装则俄语形态分析不可用，
                         #   而流水线会因此拒绝开跑（这是设计，不是故障）。
